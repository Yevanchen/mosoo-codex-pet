{
  "name": "mosoo-codex-pet",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "cf:types": "wrangler types worker-configuration.d.ts",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "dev:gateway": "bun src/dev-public-api-gateway.ts",
    "lint": "vp lint .",
    "package:skill": "bun scripts/package-skill.ts",
    "tc": "tsc --noEmit",
    "test": "bun test test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3",
    "vite-plus": "^0.1.24",
    "wrangler": "^4.107.0"
  }
}
